name: core_network
description: Handles app networking.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: '>=2.18.0 <3.0.0'

dependencies:
  flutter:
    sdk: flutter
  get_it: 7.2.0
  cupertino_icons: ^1.0.2
  json_annotation: 4.6.0
  retrofit: '>=3.0.0 <4.0.0'

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
  build_runner: 2.2.1
  json_serializable: 6.3.2
  retrofit_generator: '>=4.0.0 <5.0.0'

flutter:
  uses-material-design: true
