name: core_database
description: Handles app database.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: '>=2.18.0 <3.0.0'

dependencies:
  flutter:
    sdk: flutter
  get_it: 7.2.0
  injectable: 2.1.0
  sqflite: 2.1.0
  json_annotation: ^4.6.0
  cupertino_icons: ^1.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
  build_runner: ^2.2.1
  json_serializable: ^6.3.2
  injectable_generator: 2.1.3
  sqflite_common_ffi: 2.1.1

flutter:
  uses-material-design: true
